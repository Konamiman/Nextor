HERTZ                                                          [New]
-----

Format:      HERTZ n

Purpose:     Switches the refresh frequency of the VDP.

Use:         The only two values for n can be 50  and 60  indicating
        the refresh frequency of 50 Hz and 60 Hz.

Examples:

        HERTZ 50

             Switches the VDP to 50 Hz refresh frequency.
